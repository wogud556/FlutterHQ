void main() {
  dynamic name;
  //var name;
  if(name is String) {
    //name. name이 String으로 지정이 됐다면 String 타입의 하위 메소드가 나옴
  }

    if(name is int) {
    //name. name이 Int 지정이 됐다면 Int 타입의 하위 메소드가 나옴
    //이 if문은 항상 쓸일은 없다.
  }
}